import { DireDirective } from './dire.directive';

describe('DireDirective', () => {
  it('should create an instance', () => {
    const directive = new DireDirective();
    expect(directive).toBeTruthy();
  });
});
